extend type Query {
    "Fetch Role"
    role(
        id: ID @eq
        name: String @eq
    ): Role
        @can(ability: "view", resolved: true)
        @find

    "Fetch all roles"
    roles: [Role!]!
        @can(ability: "view")
        @paginate(defaultCount: 10)
}
